<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Android theme applied before Compose takes over. NoActionBar +
         translucent system bars suit the full-screen, landscape game UI. -->
    <style name="Theme.LearningGames" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/island_water</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
